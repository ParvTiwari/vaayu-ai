# Vaayu AI — Benchmarks

Placeholder — to be filled in once the forecast model is trained. Will
report RMSE/MAE per city per horizon against a persistence baseline, data
source coverage, and end-to-end API latency.
